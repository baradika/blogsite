---
import BlogCard from '@/components/BlogCard.astro'
import Link from '@/components/Link.astro'
import PageHead from '@/components/PageHead.astro'
import { buttonVariants } from '@/components/ui/button'
import { SITE } from '@/consts'
import Layout from '@/layouts/Layout.astro'
import { getRecentPosts } from '@/lib/data-utils'
import MusicPresence from '@/components/MusicPresence.astro'

const blog = await getRecentPosts(SITE.featuredPostCount)
---

<Layout class="max-w-3xl">
  <PageHead slot="head" title="Home" />
  <section class="rounded-lg border">
    <div class="flex flex-col space-y-1.5 p-6">
      <h3 class="text-3xl leading-none font-medium">b4r</h3>
      <p class="text-muted-foreground text-sm">
        /ˈbar/ &bull; <span class="font-medium">nouns</span>
      </p>
    </div>
    <div class="p-6 pt-0">
      <p class="text-muted-foreground mb-2 text-sm">
        CTF with @Try4gain. Interested in Reversing (i hate windows based rev challs 👿), Forensics (ye but not the file size dawg 😐), Blockchain (i love to learn it cus i think this is the future 🫡), Web Exploitation (i hate hard xss challs 🗿) and Linux (i use arch btw 🤓). 
      </p>
      
      <p class="text-muted-foreground mb-2 text-sm">
        Besides security shit, I enjoy playing roblox😜 and watching anime.
      </p>
      
      <p class="text-muted-foreground mb-2 text-sm">
        Currently studying Software Engineering (Rekayasa Perangkat Lunak) at SMK, graduating this year. Started my journey as a CTF player while learning to code, chasing puzzles like stars in the night.
      </p>
      
      <p class="text-muted-foreground text-sm">
        Currently doing my internship (PKL) at Steradian Data Optima as a Backend Developer, where I'm gaining hands-on experience in building server-side applications and working with real-world production systems.
      </p>
    </div>
  </section>
  
  <MusicPresence />
  
  <section class="flex flex-col gap-y-4">
    <h2 class="text-2xl font-medium">Latest posts</h2>
    <ul class="flex flex-col gap-y-4">
      {
        blog.map((post) => (
          <li>
            <BlogCard entry={post} />
          </li>
        ))
      }
    </ul>
    <div class="flex justify-center">
      <Link
        href="/blog"
        class={buttonVariants({ variant: 'ghost' }) + ' group'}
      >
        See all posts <span
          class="ml-1.5 transition-transform group-hover:translate-x-1"
          >&rarr;</span
        >
      </Link>
    </div>
  </section>
</Layout>
