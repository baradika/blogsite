---
import AchievementsPage from '@/components/AchievementsPage.astro'
import Breadcrumbs from '@/components/Breadcrumbs.astro'
import PageHead from '@/components/PageHead.astro'
import Layout from '@/layouts/Layout.astro'
import { getAllAchievements } from '@/lib/data-utils'

const PAGE_SIZE = 5
const allAchievements = await getAllAchievements()
const achievements = allAchievements.slice(0, PAGE_SIZE)
const totalPages = Math.max(1, Math.ceil(allAchievements.length / PAGE_SIZE))
---

<Layout class="max-w-3xl">
  <PageHead slot="head" title="Achievements" />
  <Breadcrumbs items={[{ label: 'Achievements', icon: 'lucide:award' }]} />

  <AchievementsPage
    achievements={achievements}
    currentPage={1}
    totalPages={totalPages}
  />
</Layout>
